/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // appDir is enabled by default in current Next.js versions; remove experimental flag
}

module.exports = nextConfig
